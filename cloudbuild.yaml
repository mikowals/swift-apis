steps:
- name: 'DOCKER_BUILDKIT=1 docker'
  args: [
            'build',
            '-t', 'gcr.io/$PROJECT_ID/swift-apis:latest',
            '--cache-from', 'gcr.io/$PROJECT_ID/swift-apis:latest',
            '-f', 'Dockerfile',
            '.'
        ]
images: ['gcr.io/$PROJECT_ID/swift-apis:latest']
timeout: 15000s

